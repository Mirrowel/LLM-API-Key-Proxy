# Exhaustive Audit Sweep — Findings (trio per part: light + flash + seek)

Status: batches land here as they complete. R1-R4 done. R5-R10 + per-protocol depth rounds pending.
Baseline at sweep start: 1333 passed / 0 failed. HEAD 34860c0.
These findings are ADDITIONAL to the ledger in audit-remediation-plan.md / fix-pass-plan.md (agents were instructed to read those first and hunt beyond them). Reconfirmations of known items are marked [known+] with only the new dimension noted.

---

## R1 — SHELL & ENTRY (src/proxy_app/)

### BLOCKER / HIGH
- [B] `import proxy_app.main` launches the interactive TUI whenever `len(sys.argv) == 1` — module-level TUI block (main.py:51-55) sits outside `__main__` guard; any tooling/library import with single-element argv hangs or EOF-crashes in the TUI. Duplicate TUI trigger already exists inside `__main__` block (merge + gate fixes both).
- [H] RawIOLogger does NOT redact `x-goog-api-key` (nor `api-key` Azure convention) — detailed_logger.py:109-116 sensitive set missing them; probe: Gemini client's proxy key persisted verbatim to logs/raw_io/*/request.json with `--enable-raw-logging`. Violates the module's own "bearer credentials never persisted" contract.
- [H] `_aggregate_chat_chunks` raises `KeyError: 'index'` inside the wrapper's `finally` (route_helpers.py:138) on tool-call chunks lacking `index` (real non-conforming upstreams emit them): kills terminal error frames delivered just before, and aborts even successful streams after `[DONE]` with an ASGI exception. Runs on EVERY chat stream unconditionally.
- [H] Default-key policy bypass via `UVICORN_HOST` env var (click auto_envvar_prefix=UVICORN; key_policy.py:74-89 never consults env): probe — default key served on 0.0.0.0. Also gunicorn/hypercorn `-b` binds invisible. Beyond the known `-m uvicorn` bypass [known+env-vector].
- [H-dormant] Embedding batcher (if ever flipped on): bare-string input → `item["input"][0]` embeds ONE CHARACTER per request (route_helpers.py:245 vs batch_manager.py:31) + whole-batch usage assigned to every future → summed N times (batch_manager.py:52). Both modes of the feature are wrong.

### MEDIUM
- [M] Launcher MAIN MENU prints the FULL `PROXY_API_KEY` (launcher_tui.py:458-460) — W6+W9 masking never landed on this screen; `[:20]` reset-dialog leak (launcher_tui.py:763) and quota_viewer `[:8]` (quota_viewer.py:1786) also still open.
- [M] `settings_tool.py:638` is a FIFTH `_API_KEY` substring-scan site (phantom providers from e.g. `MY_API_KEY_EXTRA`); anchored-regex fix must cover all five sites (main.py:385, launcher_tui.py:175, credential_tool.py:338, model_filter_gui.py:585, settings_tool.py:638). Ledger had four.
- [M] OAuth bootstrap is a dead skeleton: startup.py:69-78 calls `initialize_token`/`get_user_info` — zero live implementations (only `_retired/`); `DEFAULT_OAUTH_DIRS={}`/`ENV_OAUTH_PROVIDERS={}` make it unreachable today, but any OAuth file → silently dropped; launcher TUI advertises "N OAuth credentials" the runtime will never load (launcher_tui.py:184-188, :1061). Decide: delete plumbing or restore contract.
- [M] Gemini stream ingress ignores `?alt=` — ALWAYS SSE (main.py:1208-1230). Official default for `streamGenerateContent` is a JSON-array stream; `alt=sse` opts into SSE. google-genai SDK omits alt → `UnknownApiResponseError` on every streamed call. (litellm fixed the same upstream bug in PR #29019.) Fix = route reads alt + formatter serves JSON-array when absent; extends the ledger's wrong-test-pin finding with the route-level cause + SDK impact.
- [M] OpenAI-protocol error `type` strings are proxy-internal, not spec vocabulary (core/errors.py:129-135): `invalid_request`/`rate_limit`/`authentication` instead of `invalid_request_error`/`rate_limit_error`/`authentication_error`; `param` field absent; anthropic branch maps correctly — asymmetric. `test_protocol_client_surfaces.py:180` pins the deviation.
- [M] Anthropic `proxy_timeout` maps to `api_error` instead of `timeout_error` (504) — core/errors.py:105-115 has no proxy_timeout row in the anthropic map; probe-confirmed.
- [M] Non-dict JSON bodies → 500 on MORE routes than ledgered: chat (main.py:654), gemini generate (:1186), gemini countTokens (:1261 catches only JSONDecodeError/ValueError), cost-estimate, quota-stats; plus non-string `model` (int/null) → TypeError 500 via request_logger.py:25 (`'/' in model_full`). anthropic + embeddings routes are the correct in-repo template.
- [M] `get_embedding_batcher`/`get_rotating_client` use bare `app.state.X` attribute access → plain-text 500 when lifespan didn't run (main.py:538-540, 533-535); every other access is defensive.
- [M] CORS `allow_origins=["*"]` + `allow_credentials=True` (main.py:523-529): browsers reject the combo; Starlette reflects arbitrary origins w/ credentials. AND `x-proxy-session-domain` is NOT in `expose_headers` → browser clients literally cannot read the Responses capability token (main.py:839/852-855 emits it, JS can't see it).
- [M] `/v1/models` + `/v1/models/{id}` have ZERO error handling → Starlette plain-text 500 on discovery failure; `model_info_stats` returns 200+`{"error":...}` when service absent but 500 when None.
- [M] quota-stats GET/POST echo unbounded internal exception text to clients (main.py:1496, :1591 `detail=str(e)`).

### LOW / NIT (selected)
- [L] Temperature override mutates body BEFORE raw capture (main.py:646-651) — "unmodified HTTP data" logger contract violated; also fires on `false` (False==0) — route_helpers.py:208-211.
- [L] Shell stream wrapper never `aclose()`s upstream on disconnect (route_helpers.py:79-83 break without aclose).
- [L] Gemini routes have NO RawIOLogger at all (main.py:1167-1268) — debug-invisible family (and the only family whose auth header is the unredacted one above).
- [L] cost-estimate status incoherence: bad tokens → 200, missing model → 400, malformed JSON → 500.
- [L] Embeddings `request`-key collision → TypeError 500 (route_helpers.py:271-273 `aembedding(request=..., **payload)` with body key "request").
- [L] `is_localhost_bind` false-positives on 127.0.0.2 / `::1` spellings (key_policy.py:30-36 — use ipaddress.is_loopback).
- [L] Batch `input: []` → IndexError 500 with batcher on; `request.client` unguarded on chat/embeddings (:672, :1301 — anthropic has the guard); WS accepts only Bearer (not x-api-key/?key=) + close code 1000 with empty reason; startup.py: no `encoding="utf-8"`, narrow exception set, silent unknown-provider skip; multi-line `event:`-led SSE frames bypass the route log (route_helpers.py:84 startswith("data:") — must split frames not prefix-test); model_filter_gui writes `Path.cwd()/.env` (frozen-EXE broken) + `PROVIDER_SETTINGS_MAP={}` dead settings menu; launcher_config.json path convention differs between launcher (cwd) and quota_viewer (exe dir) in frozen mode; quota_viewer ships a DEBUG "inject fake window" production action; `_redact_headers` should be a shared tested helper across all carriers.
- [N] dead console_handler + double load_dotenv; misindented chat JSONDecodeError continuation (still open); stray `# noqa: F811` on WS factory (still open); empty `env_oauth_providers` loop (still open); banners print argparse-default host/port under ASGI import where they're not the real bind; `verify_*` use `==` not compare_digest.

---

## R2 — CLIENT LAYER & EXECUTOR (src/rotator_library/client/)

### BLOCKER
- [B] EMBEDDINGS BROKEN ON EVERY PATH (all three auditors, independently probed): native dispatch → `_build_native_provider_context` raises `configuration_error` 500 (`unified_request is None` — executor.py:910-913; openai declares openai_chat → the common case ALWAYS goes native); litellm path calls `litellm.acompletion` (CHAT entrypoint) not `aembedding` (executor.py:752); custom path calls `plugin.acompletion` — `ProviderInterface.aembedding` has ZERO callers; `RequestContext` has no operation field; `build_embedding_context` never sets unified_request/input_protocol; ZERO embedding tests exist. Ledger's "dormant/bypasses protocol layer" was wrong — the route is live-broken. (dev comparison: dev also mis-routed via acompletion — so broken-then, broken-now, but now it 500s BEFORE transport.)

### HIGH
- [H] `structured_api_response_error` substring classifier FALSE POSITIVES (core/errors.py:167-182): tokens `"rate"`/`"auth"`/`"quota"` matched against free-form message text — "Failed to gene**rate** content" → rate_limit 429 + rotates EVERY credential; "author"/"accurate" same. Probe-proven. Distinct from the known quota-as-400 miss: this is the false-positive direction on the same layer.
- [H] Fallback clone DISCARDS model-ID resolution for ALL targets incl. target 0 (attempts.py:32-33 overwrite with raw config string; routing never calls resolve_model_id; request_builder resolves only routing_targets[0] pre-clone): display aliases sent upstream under any routing config; session affinity ALSO dropped when `target.prefixed_model != context.model` (the resolved form). Probe-proven.
- [H] `litellm.drop_params = True` set globally (rotating_client.py:169): silent param drops on every litellm-path call contradict D7 disclosure — no warning surface exists for dropped params on that path (docs: litellm drops unsupported params silently).

### MEDIUM
- [M] Classifier-scoped (and any api_keys/providers-carrying) requests BYPASS global model allow/deny lists (model_discovery.py:96-124 short-circuits to True when classifier set and no per-request filters).
- [M] Non-streaming error sleeps IGNORE the request deadline (executor.py:2448/:2459 raw sleeps; streaming path uses deadline-aware `_sleep_before_transient_action` everywhere — parity violation; probe: 1s budget + 5s retry-after slept full 5s).
- [M] `RuntimeError: dictionary changed size during iteration` on shared usage-managers dict (quota.py:45, :115, rotating_client.py:409 iterate while ensure_scoped_usage_manager inserts — probe-reproduced 500 on /v1/quota-stats under concurrent scoped traffic).
- [M] Native path skips ProviderTransforms ENTIRELY (executor.py:410-421 returns before transforms): gemma system→user conversion, dedalus tool_choice fix, thinking-parameter delegation, plugin transform hooks — all dead on native (probe: gemma system message survives as system on native, converted on litellm). No log line says so.
- [M] L1 `assembled_stream_response` is ALWAYS chat-shaped (executor.py:2715-2836 → transaction_logger.py:898-1028): anthropic/gemini/responses streams get a fabricated empty chat.completion in traces; W12 reconstruction depends on it.
- [M] `client_protocol_context` built once, reused across credential rotations (executor.py:1878-1894 `if None` never rebuilds) — stale credential_stable_id + provider_protocol in traces and terminal error frames after rotation.
- [M] Cooldown-exceeds-budget raises raw `RoutingExecutionError` OUTSIDE all handlers non-streaming (executor.py:1405 call site; raise at :2351-2354) → generic 500, no accumulator/fallback-history entry, no rate_limit identity; dev FAILED CLEAN here (returned; no-keys path) — experimental regression. Streaming path handles it (protocol-shaped terminal).
- [M] Chat reasoning-only deltas NOT "visible" for the fallback wrapper (streaming/policy.py:133-147 omits reasoning fields; anthropic `_anthropic_visible` counts thinking; pipeline `_event_visible` counts reasoning) → target failover AFTER reasoning streamed → duplicated/partial reasoning prefixes to client. Two disagreeing visibility definitions in one request.
- [M] `stream_options: null` from client → TypeError before the loop → swallowed by catch-all → rotates every credential, confusing aggregate error instead of 400 (executor.py:1752-1755).
- [M] Multi-profile provider with NO default_profile + no class protocol_name: `_provider_native_protocol` returns None → silent LITELLM fallback — bypasses D13 fast-path-or-error entirely (resolve_profile never reached; executor.py:3048-3060 → provider_interface.py:340-371).
- [M] Malformed `provider:profile/model` addressing on the request path raises raw ModelReferenceError(ValueError) → 500 (request_builder.py:449 unguarded; `:prof/model`, `prov:/model`, `prov:bad:name/x`).
- [M] `_validate_request` raises bare ValueError → shell 500 envelope, no invalid_request identity (executor.py:2632-2650, called at :1350 outside retry try-blocks).

### LOW (selected)
- [L] `SMALL_COOLDOWN_RETRY_THRESHOLD` unvalidated env int — `="not-a-number"` → ValueError out of the error handler itself, masking the original error (4 sites: 2002/2085/2219/2421; TRANSIENT_RETRY_DELAY/JITTER ARE guarded — inconsistent).
- [L] Stream RateLimitError branch bypasses should_retry_same_key gating (raw retry_after-range check vs helper everywhere else; probe divergence).
- [L] Custom streaming branch never sets `kwargs["stream"]=True` (litellm branches do; works by accident today).
- [L] `force_refresh_quota` cred match `endswith(credential)` over-matches (`dir/monkey` for "key").
- [L] `get_quota_stats` hides zero-request providers.
- [L] `pending_chunks` buffers unbounded pre-visible output (reasoning-heavy streams) per target.
- [L] `get_model_options` precedence split-brain (reasoning_effort always overwrites; others fill-if-absent) — undocumented.
- [L] `request_id` always None in executor stream-error contexts (field doesn't exist on RequestContext).
- [L] Ad-hoc bundle fingerprints never match remove/list paths (scopes.py:220 vs :382/:407 derive differently).
- [L] Same-protocol `_format_execution_response` is identical-object passthrough — drops conversion summary + model stamp the cross-protocol path applies.
- [L] Multi-segment model + bare-id mapping drops middle segments (models.py:86 split("/")[-1] vs rebuild).
- [L] Provider `transform_request` hook failures swallowed at DEBUG (transforms.py:181-191) while built-ins re-raise.
- [L] Stream session callback is chat-shaped for ALL protocols (stream_ops.py:637-657) — G6 seam.
- [L] `_should_use_native_protocol` kwargs param dead (the mechanical root of the embeddings bug); NoAvailableKeys `proxy_busy` branch unreachable; `_terminal_stream_error_lines` hardcodes source openai_chat (trace mislabel); `AvailabilityStats`/`ExecutionResult` dead types; pre-request-callback edits to `previous_response_id`/`files` silently dropped from canonical view (executor.py:974-996 overlay allowlist).

---

## R3 — ROUTING & PROFILES (src/rotator_library/routing/ + request_builder seams)

### BLOCKER
- [B] The ENTIRE D13 JSON declaration schema is REJECTED by config validation (experimental.py:29-41 `_PROVIDER_CONFIG_KEYS` + :455-457): `profiles`, `transport_profiles`, `default_profile`, `model_protocols`, `cache_replay` all raise "unsupported keys". Multi-profile providers are expressible ONLY as code class attrs; `<NAME>_CONFIG` (D16) has ZERO implementation repo-wide. Plan §2.8's operator surface does not exist. (All three auditors probe-confirmed; one pinned the exact error strings.)

### HIGH
- [H] `@execution` split corrupts legal Ollama `@digest` model IDs (config.py:31 partition("@") unconditional): `ollama/llama3:8b@sha256:abc` → "unsupported execution mode: sha256:abc" — and as bare ValueError, not RoutingConfigError, so it escapes every config catch → 500. Split must be against the known mode vocabulary only.
- [H] Request path leaves `@native` INSIDE the model string (request_builder.py:437-455 delegates to parse_model_reference which ignores @): `myprov:chat/gpt-4@native` → model `myprov/gpt-4@native` flows into resolve_model_id/usage/field-cache dimensions. Also direct-path `openai/gpt-4@native` with no routing config → literal `@native` sent upstream.
- [H] Resolver promotion is PROFILE-BLIND (`_same_target` compares prefixed_model which omits profile — resolver.py:51-52, types.py:84-88): explicit-profile request matching a group target returns direct-single-target (failover chain discarded); bare request chains BOTH profile variants → duplicate attempts with identical history entries.
- [H] A fallback group ABORTS the whole request when ANY later target has no credentials for the caller (request_builder.py:287-300 eager pre-flight): scope with creds only for target 0 → ValueError "Invalid model format or no credentials" — target 0 never tried. The D17 multi-user case (tenant registered for some providers) breaks all routing. Must skip-and-record unserviceable targets, raise only when none serviceable.
- [H] Dynamic (env/JSON) providers have NO profile surface (providers/__init__.py DynamicOpenAICompatibleProvider: no transport_profiles, no profile-aware get_native_operation/get_native_endpoint signatures) — every explicit-profile request to a dynamic provider 400s.
- [H] Routing policy layer re-derives error class from raw 400 and hard-stops quota-as-400 AGAIN at its own layer (executor.py:3392-3403 `_route_error_type` prefers response-derived candidates over the exception's own error_type; :3416-3421 hard-stop scan walks code-derived `invalid_request` candidates BEFORE retryable scan): gemini `RESOURCE_EXHAUSTED` at 400 → invalid_request → no failover — even when details.classification says quota_exceeded. A FOURTH independent classifier defeating G1's fix at the fallback layer.
- [H-known+] `_with_request_scope` drops `profile` [known] — extended: the loss is total and UNTRACED (nothing records the drop; request-level profile can't rescue config targets; only `_target_trace` would show it).

### MEDIUM
- [M] Runtime `protocol_name` override SILENTLY WINS over an explicit `:profile` (provider_interface.py:348-350 early return): synthetic probe — profile "anthropic" + runtime protocol_name openai_chat → get_protocol_name returns openai_chat for that profile → wrong endpoint/protocol served. D13 silent-mismatch forbidden.
- [M] `provider:profile` in a MODEL ROUTE alias also loses the profile (same `_with_request_scope` omission) — config-side cousin confirmed.
- [M] Providerless `gpt-4:free` / `llama3:8b` (no `/`) misparsed as profile references → ModelReferenceError before alias lookup (request_builder.py:447 guard only checks the first segment — colon without slash shouldn't invoke profile parsing at all).
- [M] `parse_route_target` accepts profiles containing `:` (config.py:37-41) that the request parser rejects (profiles.py:82) — `a:b:c/m` config-accepted, request-400. Both parsers must share valid_profile_name.
- [M] `FallbackGroup.__post_init__`/max_targets raise raw ValueError (types.py:108-111, config.py:111) — escapes RoutingConfigError tolerance; `"two"` and `0` both probe-confirmed; `1.9` silently truncates. Second independent bare-ValueError family beyond the known @mode one.
- [M] Per-profile transport AUTH impossible: `get_native_headers` not profile-aware (provider_interface.py:537) — the planned gemini dual-profile (x-goog vs Bearer) cannot be expressed.
- [M] Plan schema says `endpoint_paths` (per-operation map); code reads `endpoint_path` singular (provider_interface.py:517-521) — operator following the authoritative schema gets conventional paths or NotImplementedError; test encodes the non-plan key.
- [M] client/gemini.py:104 `_routable_model` calls `load_routing_config_from_env()` PER REQUEST — a SECOND unguarded site outside the G7 file list (fixing request_builder.py:87 alone leaves the gemini ingress broken the same way).
- [M] Whitespace not stripped per-segment on the request path (`" openai : chat / gpt-4 "` → provider "openai " — a DIFFERENT identity in usage/cooldown/session/cache keys); config parser strips correctly — surfaces disagree.
- [M] Case preserved into case-sensitive sinks (OpenAI → registry MISS, cooldown key `provider:OpenAI` ≠ `provider:openai`); normalize-or-reject decision must cover parse + every sink.
- [M] Profile omitted from identity-bearing trace/history records (`RouteTarget.name`/`prefixed_model`/attempt-history/failure-summary all exclude it; only `_target_trace` has it) — W12 per-attempt protocol identity loses which transport served/failed.
- [M] Clone shares nested kwargs structures (attempts.py:32 dict() shallow — probe: nested mutation crosses attempts).
- [M] Clone's default usage key drops scoped pools when arg omitted (attempts.py:34 — safe today only because executor always passes it).
- [M] JSON model_routes alias keys not stripped (`' alias '` dead on arrival); env `_env_key` collision `a-b` vs `a_b` → silent overwrite; `a:b` group name unaddressable-but-accepted.
- [M] Compat-group member refs retain profiles (compat.py:35-43 `"openai:chat/gpt-4"` → provider "openai:chat") — silent default-deny; strip/reject at add_group.
- [M] Field-cache model dimension: stripped vs prefixed forms hash differently (`openai/gpt-4` ≠ `gpt-4`) — canonicalize inside build_cache_key.
- [M] No default/catch-all group: unlisted models get zero failover silently (document or add knob).

### LOW / NIT (selected)
- [L] Empty `failover_on`/`stop_on` silently disables failover (empty ≠ default; neighboring streaming_policy falls back — inconsistent).
- [L] Pre-output terminal ERROR frame can be flushed as a preamble when visible output follows, then attempt marked SUCCESS (executor.py:1158-1170 pending flush + :1171 success ignores terminal_error_type) — error-frame+content+[DONE] protocol garbage. Drop parked error frames on success.
- [L] Policy alias table doesn't cover wire vocabulary (`invalid_request_error` etc. never match — config author's own error strings silently no-op).
- [L] `RouteTarget.protocol/conditions/metadata` unsettable dead surfaces (beyond known priority/weight); `RoutingDecision.selected_target_index` dead.
- [L] Anthropic facade TransactionLogger derives provider from RAW model (`prov:chat/x` → provider "prov:chat" in trace dir).
- [N] `prefixed_model` double-prefix case-sensitive ("openai/OPENAI/gpt-4"); resolve_model_id vs strip_provider segment mismatch for nested IDs; executor `_HARD_STOP_ROUTE_ERRORS`/`_RETRYABLE_ROUTE_ERRORS` duplicate routing vocabulary verbatim (drift risk — G1 consolidation); split_profile_from_provider accepts junk; `max_targets` never enforced at runtime + undocumented in config-reference.

---

## R4 — PROTOCOL CORE FRAMEWORK (types/canonical/validation/base/registry/transforms/non-generative)

### HIGH
- [H] Base-class `build_request`/`format_response` ship STALE `raw` and ignore ALL canonical mutations (base.py:82-109, 125-141): probe — model mutated m1→m2, built payload still m1; extra dropped; format_response returns stale id. LiteLLMFallbackProtocol inherits both. Any super()-calling subclass or custom protocol silently ships pre-mutation payloads. Merge canonical deltas over raw or fail closed.
- [H] Anthropic thinking-budget clamp is BLIND to the builder's defaulted max_tokens (canonical.py:452-476 `_anthropic_budget` reads only generation_params; the 4096 default lands later in anthropic_messages.py:224-236): gemini source thinkingBudget=8192 no maxOutputTokens → `budget_tokens:8192, max_tokens:4096` = guaranteed 400, violating the function's own docstring. (Two auditors independently probe-confirmed with identical shapes.)
- [H] `output_config.effort: "minimal"` emitted to Anthropic — NOT a valid Anthropic effort level (low/medium/high/xhigh/max only): chat `reasoning_effort:"minimal"` → deterministic 400; existing test PINS the defect (test_w4:130-142 — fix the pin per W10 authorship rule).
- [H] `allowed_tools` tool-choice: canonical `allowed_names` populated with TOOL-DEFINITION DICTS instead of names (canonical.py:900-918) → cross-protocol raises ProtocolError on a VALID request (probe: chat nested shape → gemini/responses raise "references unavailable tools: [{dict}]"); gemini formatter emits dict inside allowedFunctionNames (malformed); flat Responses spelling `{"type":"allowed_tools","mode","tools":[{"name":...}]}` silently downgrades to plain `"auto"` with zero warning. Both directions broken.

### MEDIUM
- [M] `format_structured_output` anthropic branch has NO `text` case (canonical.py:1100-1114): explicit `response_format:{"type":"text"}` → `output_config:{"format":{"type":"json_schema","schema":{}}}` — semantic INVERSION, fabricated empty schema, no warning (chat/gemini branches correctly return None). Unknown types same path.
- [M] Chat usage emits non-official `cache_creation_tokens`; official spelling is `cache_write_tokens` (streaming.py:1195-1203 + openai_chat.py:1294-1295; our own responses parser documents the official spelling and prefers it — output isn't re-parseable under our own preference). Tests pin the wrong spelling (4 files). example_provider bakes it in too.
- [M] Gemini `thinkingBudget` passthrough: NO clamp, NO disclosure (canonical.py:642-643) — 60000 budget passes untouched (documented per-model ceilings 24576/32768; Anthropic-source budgets reach 64k) — asymmetric with the Anthropic branch's clamp+warn.
- [M] Gemini `thinkingLevel` emitted LOWERCASE; official proto enum is UPPERCASE (MINIMAL/LOW/MEDIUM/HIGH — canonical.py:646-651 comment claims parse leniency proves wire acceptance; tests pin lowercase; flagged needs-live-verification before shipping the case fix).
- [M] FinishReason enum drift: `UNEXPECTED_TOOL_CALL`, `MISSING_THOUGHT_SIGNATURE`, `NO_IMAGE`, `IMAGE_RECITATION`, `ESCALATION` unmapped → unknown → chat emits NO finish reason / responses fabricates `incomplete_details: max_output_tokens`. Tool-traffic ones belong with malformed_function_call → incomplete; IMAGE_RECITATION → content_filter.
- [M] `incomplete → chat(length)` approximation UNDISCLOSED (canonical.py:78-121) while its gemini twin warns — Gemini MALFORMED_FUNCTION_CALL surfaces as plain `length` with zero warning; disclosure asymmetric across all four targets for incomplete/unknown.
- [M] Hosted `web_search` modeled for Gemini equivalence but HARD-REJECTED at Anthropic (validation.py:228-229 vs :281-283) although Anthropic natively ships web_search server tools and our adapter parses+rebuilds them — Responses-client web_search → anthropic target 400s where a faithful mapping exists (mirror half of the known asymmetric case).
- [M] `resolve_tool_result_names` MUTATES the caller's canonical object (shallow list copy, writes synthetic ids into shared ToolCall/ToolResult objects — probe-confirmed) + dual-representation drift (ids on content-blocks, message.tool_calls stays empty) + no warning for synthetic-id synthesis.
- [M] Validation misses official Anthropic thinking×forced-tool-use incompatibility (forced `any`/`tool` + manual thinking = 400 per official docs; our budget branch always emits manual enabled; effort-carrying requests are safe).
- [M] `_RESPONSE_MODALITIES["responses"]` claims audio output without official grounding (official audio guide: audio-chat pattern is Chat Completions; Responses docs describe text+image-in/text-out). Reduce or cite.
- [M] Ollama `images[]` never become canonical content (ollama.py:152-153 → message.extra only): cross-protocol builds silently lose images; validation can't see them (block looks like text).
- [M] Non-generative adapters have NO validation gate: generate-build drops `messages`, chat-build drops `input`, embeddings/audio emit explicit `"input": None` (strict APIs 400 on null), operation sniffing can misroute silently, and `extra` CLOBBERS core keys (`{"model":"evil"}` wins over the routed model — precedence undocumented).
- [M] `format_stop_reason` echoes input VERBATIM for unknown target protocols (canonical.py:133-151: unknown target → table {} → returns input; known protocols safe).
- [M] Reasoning levers dropped silently in four shapes: `{"dynamic":true}` without enabled → {} at gemini home (no warning); dynamic+budget at gemini drops dynamic silently; `{"enabled":true}` alone → {} at chat AND responses with zero warning (gemini warns, anthropic upgrades silently) — the only control shape with no recorded outcome anywhere.
- [M] `canonical_tool_choice` silent coercions ([“auto”]/42→auto, {"type":"function"}→named/None) with no warning vehicle; `format_tool_choice`/`format_structured_output` on UNKNOWN targets echo canonical dicts onto the wire (probe: `{'mode':'validated'}` passes through).
- [M] `tool_arguments_object` raises bare ValueError (canonical.py:1283-1289; callers anthropic.py:803, gemini.py:826 don't wrap) → wrong error class (500 not ProtocolError 400); reachable on same-protocol builds (validation skipped) and response-side formatting (never validated).
- [M] Registry `replace=True` leaves stale alias entries owned by others (registry.py:49-62; masked only by resolve order); discovery imports canonical/validation/streaming/transforms as side effects (infra-module set incomplete).
- [M] `register_protocol` NON-ATOMIC (registry.py:45-62): name committed before alias validation → a rejected registration leaves the protocol discoverable (probe: collision error raised, `get_protocol('zzz_b')` still resolves); duplicate aliases within one class silently collapse.
- [M] Transform registry covers ONLY chat↔anthropic pairs (transforms.py:49-77): `get_transform("gemini_thinking_to_chat")` → KeyError; empty/None input to anthropic_thinking_to_chat_reasoning returns "" which the engine INJECTS as empty reasoning_content (should skip like the reverse direction); register_transform silently overwrites duplicates.
- [M] Usage-bucket exemptions only for anthropic/gemini natives (canonical.py:760-775): chat-native audio_tokens warns at chat targets — per-target native-key sets needed.
- [M] `LiteLLMFallbackProtocol` is dead-but-registered (aliases squat "litellm"/"fallback"; config allowlist rejects it; unknown operation fails the gate; nothing selects it) + `supports_transport`/`is_future_transport` have ZERO callers (transport seam never enforced — ollama declares jsonl, generative paths assume SSE).

### LOW / NIT (selected)
- [L] Responses `cancelled` status unmapped → cross-protocol conversion of a cancelled response misreports (chat: no finish; responses: fabricated incomplete+max_output_tokens).
- [L] Chat stream tool-call continuation chunks emit `"function":{"name":null,...}` (None-filter is top-level only — streaming.py:884-887; real OpenAI never sends name:null).
- [L] `effort_from_budget_tokens` never returns xhigh/max (asymmetric with effort→budget which emits 65536 for max).
- [L] `normalize_reasoning_controls` docstring overstates folding (gemini folding actually lives in gemini.py); MCP builds `method: None` (spec requires method on requests); Ollama system list → str() garbage literal `"['a','b']"`; Ollama embeddings-prompt detection unreachable via auto-sniffing; audio translation unreachable without explicit op.
- [L] `serialize_value` set-ordering nondeterministic + Decimal→float precision loss (snapshot instability); request-warning dedup ignores source_protocol (rotation reuse).
- [L] `Usage.__post_init__` cache-only total=0 — document the convention; `tool_result_object` invents `{"result": scalar}` key against schema without disclosure; registry case-sensitive vs config case-normalizing (document canonical lowercase).
- [L-known+] `_stream_warn` dedup key ignores `field` [known] — confirmed: identical drops on different fields collapse to one warning.

---

## R5 — NATIVE EXECUTION + ADAPTERS + FIELD CACHE

### HIGH
- [H] Portable `model:<name>` pooling is DEAD by construction: `can_inherit()` ORs the synthetic identity group, but the engine's `siblings()` only scans explicitly-declared group members — empty registry → `siblings()==[]` while `can_inherit()==True` → same-model-cross-provider portable rules always miss (compat.py:98-119 vs :75-96; engine.py:297-333; probe: cache_miss with reason=cache_miss). Plan 2.7's "same model, any provider" pool doesn't exist.
- [H] `unified_request`-target injection of `messages`/`system`/`tools` is SILENTLY DROPPED (native_provider/executor.py:686-715 safe_fields allowlist) while the FieldCacheOperation reports `changed=True` and traces log the injected payload — engine lies, wire never sees it, AND the raw fast path is disabled + a pointless `unified_state_injection` overlay recorded (probe-confirmed by two auditors).
- [H] Native transport owns NO timeouts — shared bare `httpx.AsyncClient()` (rotating_client.py:265) + no timeout plumbing anywhere (http.py:20-73): httpx default = 5s network inactivity → non-streaming generations >5s die as raw ReadTimeout; TTFB >5s (reasoning models!) dies; no error class, no rotation identity. Litellm path uses TimeoutConfig 30/300/600 — native (the DEFAULT path) is 60-120× stricter by accident.
- [H] `: cost` SSE comments DROPPED on native streams (http.py:154-171 comments → []) — provider-reported cost lost; `event: cost` survives only by accident (generic dict pickup). Plan 2.5 lists both as preserved machinery. Also: provider `: keepalive` comments never reset the stall clock (false stall risk).
- [H] `cache_replay` vocabulary drift vs plan 2.9: `turn_only` and `last:N` REJECTED (`keep must be last|all|turn|turns:N|per_tool_call`); `turn` compiles to last_user_turn ≠ plan's turn_only=since_last_user multi-value. config-reference documents the drifted vocab as contract.
- [H] `insert: true` + default `if: auto` NEVER inserts (paths.py:144-152 `when_missing_only` short-circuits before insert) — every declarative insertion is a silent no-op unless `if: always` (probe ×2 auditors).
- [H] `FieldRenameAdapter.stream_event` stage is DEAD on native (builtin.py:140-167 `if not isinstance(payload, dict): return` — native passes StreamEvent objects) — every `stage: stream_event` rename rule silently no-ops (W7 pins stream adapters on neutral events; the only path-based builtin can't operate there).
- [H] Envelope + cache-injection KILL confirmed (concrete mechanics of the known ordering bug): wrapped payload → `inject_path("contents.0...")` raises FieldCachePathError → request dies; top-level path → creates sibling of `request` that never reaches provider (silent loss). No response-unwrap stage exists either.
- [H] Stream usage merge is REPLACE not field-merge (native executor.py:594-614 `selected = candidate`; stream_ops StreamUsageTracker._reduce same) — Anthropic split usage (input on message_start, output-only on message_delta — the REAL wire shape) loses all input/cache tokens in ACCOUNTING while the client wire is correct. Existing parity fixture fabricates input_tokens on message_delta — a shape the real wire never sends (fixture-shaped-fix failure mode AGAIN). → money G5, accounting G4.
- [H] `mode: all` without `as_list` injects a LIST into a scalar path (engine.py:489-494) — default `turns:N` (compiles to all+max_values) injects `["a","b"]` where a string is expected; no validation.
- [H-graded-M by one] Compat groups: JSON `compatibility_groups` surface DOES NOT EXIST (only env FIELD_CACHE_COMPAT_GROUPS); the PLAN'S OWN EXAMPLE VALUE (bare model names) is REJECTED by add_group (requires provider/model) and the ValueError disables ALL groups ("compatibility groups disabled") — a documented surface that silently kills the feature. No curated registry shipped (docstring lies).

### MEDIUM
- [M] Same-protocol response passthrough drops D7 request warnings (raw branch returns deepcopy(raw_response), never the unified response carrying warnings).
- [M] Stream path records NO transport basis/overlays at all (only include_usage) + no request-warning capture — W12 stream reconstruction carrier missing entirely.
- [M] `provider_state_compatible` computed differently on the two paths (non-stream: injection-happened; stream: ORs same-protocol+input_provider — the routing-derived identity defect-9 family). Single helper needed.
- [M] `when_missing_only` None-semantics inconsistent: present-None BLOCKS auto on dict keys, ALLOWS overwrite on list indexes (paths.py:128-160).
- [M] Transform empty-string asymmetry: anthropic→chat("") returns "" which the engine INJECTS as empty reasoning_content (chat→anthropic correctly skips; engine scalar path skips only None).
- [M] adapters/registry: `replace=True` silently steals aliases from another owner; alias-collision failure leaves GHOST registration (name committed before alias validation — non-atomic, same class as protocols registry).
- [M] Unknown adapter names → raw KeyError PER REQUEST (no startup/config validation of adapter_names).
- [M] `ModelOverrideAdapter` fabricates a `model` key on keyless protocols (gemini gets `{"contents":[], "model":...}` — model rides the endpoint there).
- [M] `FieldRenameAdapter move=True` on indexed paths silently COPIES (delete can't handle indexes → source kept, no warning).
- [M] `post_json` 200-with-non-JSON → raw JSONDecodeError out of the transport (500, unclassified); text-fallback pattern exists only on the error path.
- [M] 3xx redirects → `invalid_request` (client-blamed) — httpx defaults follow_redirects=False; should follow or classify transient.
- [M] Provider heartbeats swallowed by transport (`:` → []) — stall detector sees only parsed events → healthy heartbeat-only connection can trip stall rotation.
- [M] `add_group` overwrite leaves STALE membership (removed members keep inheriting — D12 default-deny violated).
- [M] Cache keys not canonicalized: provider case (`OpenAI`≠`openai`), model case/prefix → split pools on trivial skew (engine.py:92-113 raw hash).
- [M] Transport profile NOT in cache key — chat-profile reasoning restores verbatim into responses-profile request of same provider+model (cross-profile shape sharing unguarded unless operator configured a transform).
- [M] `compile_cache_replay` fails late and loose: malformed path compiles then raises per-request; string scope `"provider"` silently → DEFAULT_SCOPE; unknown transform raises KeyError not ValueError; ttl accepts negatives (store treats ≤0 as INFINITE); `enabled: "false"` (string) is TRUTHY → enabled.
- [M] JSON `field_cache` vs `cache_replay` inject DEFAULTS DISAGREE: JSON default when_missing_only=False (OVERWRITE) vs replay auto=preserve — same operator intent, opposite behavior per surface.
- [M] `max_values`/`max_bytes` violations kill the request (store raises; engine re-raises; no containment) — one 5MB reasoning value = post-success 500. Corrupt ProviderCache JSON row = permanent per-key 500. (Containment rule confirmed by all three with new trigger shapes.)
- [M] `ProviderCacheFieldStore`: TTL never evicts from backing cache (unbounded dead blobs); `clear()` wipes the ENTIRE ProviderCache (field-cache + everything else); `_APPEND_LOCKS` keyed by `id(cache)` — id reuse cross-pairs locks after GC.
- [M] Continuation-strip checks only the LEAF path segment — nested continuation paths (`a.previous_response_id.b`) survive suppression and ship provider state after proxy expansion.
- [M] `metadata`-target injection has no reserved-key guard — a rule can overwrite `execution_profile`/`input_provider`/`disable_provider_continuation` and flip routing/continuation decisions post-strip.
- [M] `scope_key`→`conversation_id` mapping is a DEAD SEAM (no scope reads conversation_id; with session_id=None everything pools at _none regardless of scope key).
- [M] `allow_missing_session` knob is INERT (never read by the engine; only compared in the signature/guard).
- [M] `_redact_leaf_key` over-redacts: redacts EVERY same-named leaf in traces (client-supplied values indistinguishable from cached ones).
- [M] `PayloadAdapter.transform` typo-guard unreachable (`stage not in supported_stages → return payload` precedes the raise) — unknown stage silently no-ops for every adapter.
- [M] `ReasoningContentAdapter` copies EMPTY strings (presence check not truthiness) — empty reasoning_content ships upstream.
- [M] `_hydrate_unified_request` safe_fields also omit `warnings`/`logical_operation`/`source_protocol`.
- [M-low] replay cannot declare provider-continuation rules (compile never sets the metadata flag → any previous_response_id inject raises); `_wire_view_matches_unified` omits `input`/operation identity; `last_user_turn` returns the most-recent value-bearing turn not the latest turn (stale earlier-turn state attaches to newer turns); scope-tuple ORDER changes the cache key; stream flag forced True with no overlay; include_usage forced over explicit client False without disclosure; sibling fan-out unbounded N store.gets per miss; adapter singleton instances shared across concurrent requests (undocumented for stateful customs).

### Known-reconfirmed (still open, one line each)
Finalizer-before-adapters ordering (+H8 mechanics above); `done` bypasses stream adapters + extractions; no assembled-response extraction on streams; `_shared_cache_signature` omits compatibility+transform (bound→portable laundering probe CONFIRMED end-to-end); D11 floor unenforced on raw plugin rules (`scope=("session",)` shares one key across providers); weakening guard ignores `enabled`; SSE id:/retry: garbage events; no finally/aclose on native stream loop; post-injection extract("request") re-captures injected values (echo); no config-file compat groups/exclusion/curated registry; 3xx/suppression leftovers; adapter transaction_logger=None (2 sites); native CostCalculator() bare (G5); `cache_replay`/profiles keys rejected by config validator (R3-B).

---

## R6 — STREAMING END-TO-END

### BLOCKER
- [B] Responses-target block identity COLLAPSES `text A → tool → text B` into `AAABBB` for CHAT and GEMINI sources (streaming.py:804-853 `_event_block_index` returns a key whenever output_index OR content_index set — chat stamps output_index=choice_index and gemini output_index=candidate-index on EVERY event, so the family-epoch repair is DEAD CODE for all four real adapters; docstring claims identity-less wires return None — false). Two auditors probe-confirmed independently (chat+gemini sources; reordered "BBB" before the tool call; reasoning→text→reasoning same collapse). The W4 test that "covers" it uses hand-built events WITHOUT indexes — the fabricated-fixture failure mode again.
- [B/H] Gemini same-name tool fragments split into separate calls then HARD-FAIL: id-less/index-less fragments of ONE functionCall key as `tool:name#0`/`#1` (occurrence counted per EVENT), buffer separately, emit ZERO frames, terminal raises ProtocolError("incomplete streamed tool-call argument") — a VALID provider stream becomes an error (streaming.py:804-834).

### HIGH
- [H] `_format_openai` does NOT skip `.done` provider snapshots (every other formatter routes through `_event_blocks` drop; openai uses delta directly) → Responses→Chat DUPLICATES content and tool calls (probe: "Hello" emitted twice incl. output_item.done re-emission). Matrix test omits output_item.done events from its source frames — passes vacuously.
- [H] `_event_visible` checks `delta.text`/`delta.reasoning.text` on a UnifiedMessage that HAS NO such attributes (only tool_calls ever visible) → TTFT always None, visible_chunk_count always 0, visibility traces wrong for text/reasoning (stream_ops.py:675-690). Two auditors probe-confirmed. (Ledger's claim that pipeline counts reasoning — wrong.)
- [H] Spurious `data: [DONE]` appended to EVERY Responses terminal incl. response.failed — official Responses grammar has NO [DONE] sentinel (chat needs it; anthropic/gemini correctly don't).
- [H] Bare-EOF handling: synthetic terminal fabricates a VALID-LOOKING completion per protocol (chat: no finish_reason chunk; anthropic: stop_reason:null in message_delta — only legal mid-stream; responses: incomplete_details.reason="max_output_tokens" whenever stop unknown — truncation labeled as max-tokens; gemini: usage-only candidates-less husk frame) AND `mark_success`+`success_callback` run on bare EOF regardless of completion evidence — truncated stream = full quota/cost success (stream_ops.py:581-597, :622-635). Responses bridge path worse: stores the truncated response as COMPLETED continuation context. (Extends the ledger's anchors-only note: client framing + accounting + storage all wrong. NOTE: user ruling #25 = repair-not-fatal applies — repair rules + honest stop synthesis + success only on evidence.)
- [H] D9 violated on streams: multi-candidate → Anthropic target CONCATENATES candidates silently (non-stream does first-wins+warning; stream emits both text blocks, zero warnings) — streaming.py:303-405.
- [H] ChatWireStreamAdapter drops n>1 siblings on the PLAIN-DELTA path too (:295 fallthrough `return [event]`) — beyond the ledgered usage-path drop; combined with native singular parse, BOTH live sources drop candidates on ordinary content chunks.
- [H] Shell `_stream_error_frames` has NO Responses branch (falls through to chat shape `{"error":...}+[DONE]` — invalid for Responses clients) and ALL branches hardcode 500/server_error discarding the executor's classification (route_helpers.py:33-58).
- [H] Executor logging wrapper blind to `event:`-framed families: anthropic streams → ZERO chunks/assembled logged; responses → only the spurious [DONE]; gemini → fabricated chat.completion (executor.py:2740-2795 + L1 assembled absent/lying for 2-4 of 4 protocols — extends the known always-chat-shaped item).

### MEDIUM
- [M] Anthropic-target `message_start` emits `input_tokens: 0` whenever usage arrives late (chat/gemini/responses sources) — message_delta only carries output; client-visible input tokens lost for cross-protocol anthropic streams.
- [M] Stall gate counts DOWNSTREAM backpressure: `record_event` stamps before the yield — slow consumer (1 frame/s) + healthy upstream (0.2s cadence) + 0.3s stall window → stream killed (probe). Stall must measure only time blocked on `__anext__`.
- [M] Cross-protocol tool-call `index` for chat target = SOURCE block/item index (anthropic content-block index starts at 1 after text; responses global output_index) not 0-based call-array position — strict SDK assembly breaks.
- [M] Gemini error envelope diverges between terminal paths (canonical formatter passes error dict verbatim vs shell's official {code,message,status} gRPC shape) — same failure, two wire shapes.
- [M] Neutral `error`-type events neither terminate the pipeline nor block success accounting (excluded from terminal types + completion signal; formatted+yielded then loop keeps waiting; finally mark_success with zero usage) — dormant (live sources raise) but the gate is wrong for custom sources.
- [M] Transport neutrality re-entrenched in SSE: pipeline hardcodes SSEStreamFormatter for heartbeats, traces hardcode transport="sse", supports_transport zero callers, WebSocketStreamFormatter/ProtocolStreamConverter/convert_protocol_stream ZERO production callers (dead seam) — WS transport cannot be added without rewrites (plan 2.5 hard constraint).

### LOW / NIT (selected)
- [L] Responses annotations: parser omits content_index + formatter has no citations branch → phantom empty item + citation dropped even same-protocol (the DIAGONAL cell).
- [L] Duplicate gemini usage-only frame after finish-carrying frame; `StreamFormatState(None)` never persists (latent extension footgun); completion gate over-accepts `response.failed` as signal (dormant); formatter state survives credential retries (open blocks/response_id leak into retry attempt); adopt() unconditional replace; `cancel_upstream_on_disconnect=False` abandons generator to GC; `StreamedAPIError(data=<exception object>)` degrades payload; dead JSONDecodeError branch; duplicate helper sets between streaming.py and stream_ops.py (_usage_from_sse_*, _merge_streamed_tool_arguments, _assembled_tool_calls); `wrap_stream` reachable only from tests (timing suite drives retired handler — known); `native_stream_event_from_formatted` + `StreamMonitor.is_stalled` + `STREAM_TRACE_METRICS` dead; `[DONE]` after chat error frames (nonstandard trailer).

---

## R7 — RESPONSES API + WEBSOCKET

### HIGH
- [H] Cross-lane fork failure EVICTS the shared parent from the WS local cache (websocket.py:511-514 unconditional pop on any response.failed; official guide: same-lane failure evicts, cross-lane fork failure PRESERVES the parent so the source lane can continue) — probe: lane A completes, lane B forks from P and fails, lane A continuation 404s. `LaneState.latest_response_id` is written-never-read — per-lane tracking is exactly what's missing.
- [H] Store failure on a SUCCESSFUL stream terminal: `_store_stream_response` unguarded inside the passthrough loop → exception falls into outer except → `response.completed` then `response.failed` DOUBLE terminal on one stream (service.py:510-529). Non-stream create: store failure RAISES → client loses the completed response entirely, route renders 500 + str(e) (service.py:288-294) — probe-confirmed (two auditors). G11 store-never-fails violated on both paths.
- [H] Poisoned provider-cache row with non-numeric `expires_at` escapes the corrupt-row guard (`from_dict` guarded, `is_expired()` at :149-151 OUTSIDE the try) → TypeError 500s / WS response.failed carrying the raw TypeError text; one tampered row DoSes the id AND every chain through it.
- [H] Proxy-synthesized Responses objects are UNPARSEABLE by the official OpenAI SDK: every failure terminal, warmup frame, and cross-protocol format_response omits REQUIRED fields (`created_at`, `parallel_tool_calls`, `tool_choice`, `tools`; usage detail objects zero-omitted) — `Response.model_validate` raises 4+ ValidationError per object (probe against installed openai SDK). input_items returns `{"object","data"}` without `first_id/last_id/has_more`, and raw client input (bare strings) stored as items. The DIAGONAL same-protocol path passes provider objects verbatim — only the proxy's own synthesis breaks strict clients (two auditors).
- [H] Legacy bridge path fabricates `response.completed` on truncated streams (bare EOF AND finish_reason:length → completed, never `incomplete`) + STORES the truncated response as valid continuation context (service.py:987-1168; no completion-evidence gate on this path) — the Responses-local twin of the R6 bare-EOF family.
- [H] Provider `response.completed` WITHOUT a response object (bare event) → yielded verbatim THEN punished as terminal-less → completed + response.failed double terminal (terminal detection requires truthy payload; :508-510 + :556-575).

### MEDIUM
- [M] WS failed turns silently LOSE smart capture-on-error: `_turn` breaks at terminal + `aclose()`s the generator → `_finalize_stream_metadata(error=...)` + `flush_capture_on_error` never run (probe: HTTP SSE failure → full capture; identical WS failure → bare finalize only). D15 violated on WS.
- [M] Official-grammar top-level `error` stream event is NOT terminal on the native path (terminal set = completed/failed/incomplete) → error frame passed through THEN synthesized response.failed "ended without terminal" — double terminal + lying diagnostic + provider error context not stored (live path, unlike R6's dormant pipeline gate).
- [M] Capability-token rotation ORPHANS earlier chain members: each create mints a fresh token; GET/DELETE require the hash of the token issued at THAT response's creation → chain turn 2 (token T2) cannot GET turn 1 (probe: 404 with T2, 200 with archived T1) — the natural keep-newest-token client pattern silently loses history access. Token behaves per-request, not per-scope.
- [M] WS error frames omit the documented inner `error.type`; stream-id violations use `invalid_request_error` instead of `invalid_stream_id` (guide example verified verbatim; docstring pins the pre-change shape; test pins the deviation).
- [M] provider_cache "durable" store: 48h hard TTL regardless of configured TTL (disk_ttl default) → chains silently 404 after 2 days with misleading hint; one corrupt JSON file → whole store silently reset ("starting fresh"); every save rewrites the ENTIRE single-JSON file O(N); no max_items on this backend.
- [M] `input_items` returns raw un-normalized input (bare strings as data), no pagination params honored (after/limit/order/include), wrong envelope (see H-SDK).
- [M] Warmup: pins scope to "public" (scoped chains 404 — known G11), validates NOTHING (no model required — HTTP paths require it; garbage previous_response_id stored and silently laundered later by lineage truncation), and does NOT preserve tools/instructions contrary to the official purpose ("cached state includes tool definitions").
- [M] Lineage: silently truncates at missing middle (deleted grandparent → shorter context, no signal) AND at max_depth=20 (25-chain → last 20, untraced) — invalid/deleted ids become silently-wrong contexts billed as full. Chaining from `failed`/`in_progress` parents allowed (no status guard; failed ids become valid chain links via WS local cache).
- [M] Native failure terminal mints a FRESH response id (created resp_prov ↔ failed resp_Paf9 correlation broken; legacy path reuses correctly).
- [M] Native stream loop: no disconnect check, no finally/aclose — leak on EVERY turn (success, failure, disconnect alike); legacy path has both (asymmetry).
- [M] WS internal errors misclassified 400 invalid_request_error with raw exception text (websocket.py:302-322 defaults; poisoned-row TypeError reached the client) while HTTP gives 500 for the same fault — three different stories for one fault.
- [M] InMemory store default is UNBOUNDED (ttl_seconds=None, max_items=None; store=true default) — every response retained until restart; D17/multi-user readiness contradiction.
- [M] created_at taken from PROVIDER payload for LRU eviction ordering → a provider stamping year 5138 pins its row forever (eviction gameable).
- [M] WS single-flight vs official multiplexing: run() awaits each turn; official current guide documents concurrent lanes (16 in-flight / 32 named) + steering; docstring pins the sequential revision as current (stale); `response.steer` rejected. Decision needed: implement multiplexing or document the downgrade explicitly (G11 variant split).
- [M] Sequence-number burn: response_completed_payload allocates+discards one number; _current_stream_payload burns TWO per call (nested item built via the sequencing builder) — intra-payload gaps beyond the known cross-domain mixing.
- [M] Responses usage/cost path uses bare `CostCalculator()` (no provider_plugin — service.py:1486) — same-family as the known stream gap; plugin-priced models report different costs on the Responses surface.

### LOW / NIT (selected)
- [L] No HTTP cancel route (official exists; background-only so currently unreachable — but SDK cancel() gets plain 404; ALSO: official WS has NO response.cancel client event — the ledger's "implement response.cancel on WS" directive should be DROPPED per current docs).
- [L] `ResponsesServiceError.to_protocol_payload` puts the HTTP status NUMBER in `code` ("404") — official codes are machine strings or null; WS limit frame sent outside the send-guard; `_stream_failure_error` forwards raw str(exc) to clients; `_response_to_dict` repr() fallback then crashes .get on non-mapping returns; DELETE object literal "response.deleted" vs official "response" (pinned deviation); native SSE success has NO [DONE] but failure path appends one (same stream frames differently per outcome); store accepts any truthy value ("false" string stores); typo'd previous_response_id with store=true persists junk failed rows under store_failed default-true; `MAX_CONNECTION_SECONDS=nan` passes the guard and starves the WS loop (needs isfinite); WS disconnect-mid-turn records metadata 500; GET/DELETE/input_items routes lack catch-all (store I/O faults → plain 500).
- [L] config-reference documents RESPONSES_STORE_FAILED default false; code default TRUE (operators get failure persistence they were told is off); config-reference points at /v1/responses/ws (actual: /v1/responses).

---

## R8 — PROVIDERS & INTERFACE

### HIGH
- [H] NVIDIA JSON-config identity split-brain: `_provider_config_key()` derives "nvidia" (class name) while the registry remaps to `nvidia_nim` — JSON `providers.nvidia_nim.*` is accepted but read by NOBODY (rules silently dead); `providers.nvidia.*` is treated as a NEW custom provider → missing api_base → **startup failure** (probe-reproduced via real registration path). Three-way identity split with executor env keys too.
- [H] Mistral `get_models` parses the WRONG ENVELOPE: official `/v1/models` returns a BARE ARRAY; code expects `{"data":[...]}` → AttributeError swallowed per-credential → Mistral model discovery always returns [] (the "all ten declarations correct" verdict was wrong — two auditors verified against official OpenAPI).
- [H] Cohere discovery hits the WRONG HOST: code uses `api.cohere.ai/v1/models`; official List Models is `api.cohere.com/v1/models` (the `.ai` host is the compat base — chat base correct, discovery host confused). Also single-page only (no next_page_token loop; default 20 — silent catalog truncation; Gemini same: default 50/page unpaged).
- [H] Env-only dynamic providers silently stay on LITELLM (no protocol_name declared → gate False; W11 explicitly requires "default native openai_chat"; dead `or "openai_chat"` endpoint default proves the intent) — probe ×2 auditors. JSON-defined dynamics: non-stream goes native, streams silently stay litellm (native_streaming_supported undeclared) — one target, two runtimes, undocumented.

### MEDIUM
- [M] DeepSeek custom path FABRICATES `reasoning_content: "Reasoning content unavailable."` into upstream history on cache miss (placeholder as the model's own reasoning, debug-log only) — official API requires verbatim pass-back; omission → 400; fabrication pollutes chain-of-thought. Also: repair gated on tool_calls-bearing turns, but official requires pass-back on ALL turns of a tools conversation.
- [M] DeepSeek/NVIDIA reasoning-effort ESCALATION contradicts official API: V4_EFFORT_MAP maps low→high, high→max, unknown→max (official accepts low/high/max directly + maps medium/xhigh itself) — client-requested control silently overwritten (cost/latency impact). Mistral thinking handler REPLACES client reasoning_effort with hardcoded "high" (official accepts none|minimal|low|medium|high|xhigh — the workaround only needed relocation, not overwrite).
- [M] Profile `endpoint_path` placeholders `{model}`/`{operation}` are NEVER rendered by the base get_native_endpoint — literal `{model}` goes on the wire (the dynamic provider's override renders; the base doesn't) — blocks the gemini profile work.
- [M] Env-dynamic + JSON transport key = hard ValueError ("implemented in code; custom transport keys not applied") — misclassification; D16 says env knobs override same-named CONFIG values; instead the combination bricks `import rotator_library`.
- [M] `supports_native_streaming` default branch ignores `operation` (override branch checks it) — contract asymmetry, unreachable today, wrong for future callers.
- [M] NanoGPT subscription-model fallback over-includes: on fetch failure ALL discovered models join the monthly quota group → unrelated exhaustion syncs cool unaffected models.
- [M] Firmware quota baseline omits max/used/group (chutes+nanogpt set them) + reaches into usage-manager internals for direct cooldown — double path, inconsistent with siblings; firmware quota-group member returns PREFIXED "firmware/_quota" (contract says unprefixed) → double-prefix key never tracked → timing sync silently no-ops.
- [M] Dynamic anthropic endpoint default `/messages` vs interface default `/v1/messages` — no documented base convention; dormant but broken either way once used.

### LOW / NIT (selected)
- [L] Static get_models crash on ONE malformed entry (model['id'] direct) — whole credential's discovery fails vs dynamic's safe .get (all 9 static providers); `_retired/*` import-path fixes INCOMPLETE (6 modules unimportable — wrong relative depth + missing symbols; plan says "mechanical keep" was done); `openai_compatible` registry key vs `_provider_config_key` mismatch (openai_compatible vs openaicompatible — JSON config never reaches it; and it + example both crash on no-arg instantiate while registered routable; example advertises a live background job); ProviderCache._env_int crashes on malformed env (4th unguarded site); quota fetchers without timeouts (chutes/firmware no-client branches); NANOGPT_FALLBACK_MODELS stale hardcoded catalog (fail-honest instead); `import litellm` loads .env as a side effect → PROVIDER_PLUGINS varies by CWD (ambient .env changes the registry); NanoGPT enforceDailyLimit always true per docs (daily "soft" claim stale); env QUOTA_GROUPS_* cannot CREATE groups (JSON can — asymmetry); ProviderCache create_task without running loop → RuntimeError for sync users; base_quota_tracker ~870 lines zero live subclasses.

---

## R9 — CROSS-CUTTING (usage/costs/sessions/errors/logging/config)

### HIGH
- [H] **usage.json persists raw API keys in plaintext** (`credentials.<id>.accessor` + `accessor_index` keys — storage.py:164-169, manager.py:297) AND `GET /v1/quota-stats` serves them back verbatim via `full_path` (manager.py:989-993 → quota.py:55 → route; probe: `sk-SUPERSECRETKEY...` returned as full_path behind only the shared proxy key). Any proxy client can enumerate every upstream credential. Pre-existing on dev but unledgered.
- [H] Provider `on_request_complete` hook exceptions escape `CredentialContext.__aexit__` unguarded (hooks.py:232 + manager.py:784-791/109) — a buggy hook destroys completed responses AND skips all accounting for that provider (probe ×2 auditors).
- [H] **Gemini usage accounting drops thinking tokens + all toolUsePromptTokenCount + the authoritative totalTokenCount** (accounting.py:257-278: unconditional `completion -= reasoning` + recomputed total; litellm's own reference handles inclusion with `is_candidate_token_count_inclusive` and trusts totalTokenCount — probes: total 1500 vs 1600, 368 vs 616). Window metrics undercount on a first-class provider.
- [H] **Fair-cycle duration expiry NEVER resets the cycle** (fair_cycle.py:110-112 `_should_reset_cycle` returns but nobody performs the reset; reset only fires when available is empty — after expiry everyone is allowed so it never fires): exhausted stays sticky, cycle_start never advances, and with the DEFAULT 7-day duration + default-enabled fair cycle, enforcement silently dies for the process lifetime after the first window elapses (probe).
- [H] Poisoned usage.json window fields crash every subsequent accounting (`request_count: "12"` string → TypeError in record paths, escapes via the unguarded __aexit__; parse-time type validation absent) — the R7 expires_at class in a new file.

### MEDIUM
- [M] `log_response`/`log_transform_pass` persist caller-supplied headers verbatim (transaction_logger.py:758/767 — no sanitize; latent today, no production callers pass headers, but the invariant isn't enforced on this boundary).
- [M] BufferedWriteRegistry pop race drops the NEWEST buffered write (unconditional pop after lock-free write — probe: OLD on disk, pending empty, NEW gone; the registry exists to preserve latest-state during disk outages).
- [M] Error records (`metadata.json` errors + provider `error.log`) are unredacted free text — `str(error)[:2000]` with embedded URLs (litellm gemini auths via `?key=`); text scrubber misses bare `key=` params.
- [M] Capture-on-error OVER-captures transport classes (D15 says no-capture for transport/timeout; exclusion set omits api_connection; httpx ConnectError/ReadTimeout probe-confirmed capturing) AND misses numeric-code rotation errors in wrapped data (`StreamedAPIError(data={"error":{"code":429}})` captures). Invert to the enumerated inclusion set.
- [M] `applies_to="credential"` windows: produced, enforced NOWHERE (documented provider contract; next port trusting it gets silent no-op).
- [M] Model-scoped cooldowns invisible to both "soonest cooldown" helpers (checker checks model+group+global; soonest/shortest check group+global only → NoAvailableKeys instead of waiting seconds; probe).
- [M] httpx 404/408 collapse to non-rotatable invalid_request (408 contradicts the capture layer's own rotation classification; `not_found` is dead vocabulary no classifier produces).
- [M] Duration parser has no day component (error_handler vs usage/config divergence — `2d5h` → None → wrong cooldown tier for Google-style quota resets).
- [M] `_get_usage_count` falls back to LIFETIME totals on expired windows (balanced weighting inherits lifetime bias after every rollover; probe).
- [M] Fair-cycle quota-threshold exhaustion mutates state inside READ-ONLY checks (get_blocking_info flips exhausted=True — monitoring scrapes can exhaust credentials).
- [M] Explicit config path silently = no config (typo'd LLM_PROXY_CONFIG_FILE → defaults, zero log — fail-open against the #26.3 direction).
- [M] Invalid STREAM_* booleans raise out of settings getters (STREAM_TRACE_METRICS="" 500s every stream; sibling knobs warn-and-default).
- [M] Client+provider trace writers share transform_trace.jsonl via racy read-modify-write (lock covers only compress); provider boundary files bypass zstd entirely (plain json.dump — D15 "all artifacts zstd").
- [M] CostCalculator._litellm_cost stuffs the WHOLE total into output_cost (input=0/cache=0 for every litellm-priced model in per-bucket consumers).
- [M] ResilientStateWriter/registry catch only OSError (serializer TypeErrors propagate — probe: circular ref ValueError escapes; background retry thread dies for ALL pending files).
- [M] JsonlBuffer drops the WHOLE buffer on one oversize entry (probe: 5 entries + 1 oversize → 0 kept; ring-buffer pattern exists in transform_trace, not applied here).
- [M] Quota sync max-wins upward but limit overwrite is unconditional (stale-high local counts never correct down; false exhaustion after server-side resets).
- [M] Session count unbounded in memory AND save (`_sessions` grows 1:1 per unique request; only LOADED sessions capped at 10000 — probe: 3000 requests → 3000 sessions persisted).
- [M] classify_error can't parse structured errors in raw JSON-STRING data (StreamedAPIError(data=str) path → unknown) and `_classify_structured_error_text` doesn't know wire `*_error` vocabulary (authentication_error/overloaded_error/insufficient_quota → unknown).
- [M] Explicit cooldown_mode/max_requests_mode corrupt signs and percentages (`-300` with mode=offset → +300; `80%` → offset 80 — sign stripped before parse; probe).
- [M] Partial env pricing override WIPES all other JSON pricing fields (one MODEL_PRICE_* key → siblings zeroed; D16 says per-field override).

### LOW (selected)
- [L] Monthly window reset off-by-one-month when first use precedes 01T03:00 (latent); weekly/monthly ignore daily_reset_time_utc (hardcoded hour=3); status endpoint can never report "exhausted" under credential tracking mode (`_credential_` vs group-name comparison); model-window limit sync split-brained (record path omits limit); native rate-limit headers case-sensitive ingest; per-tier fair-cycle reset ignores priority overrides; ProviderLogger.log_extra unsanitized + path-unsafe (`../../` escapes log dir); zstd stale-read ordering (plain fallback beside older .zst → stale reads win) + additive merge double-counts on crash; TransformTraceWriter flushes every entry while sequence<100 (O(n²) rewrites); corrupt usage.json total-discarded on next save (quarantine instead) + Windows non-atomic move chain; acquire_credential(deadline=0.0) default raises immediately (docstring example broken); cost falsy-zero (`0.0 or price` → full price) + uncontained ModelPricing(**dict) TypeError; prune stat-storm per request (up to 1000 stats per TransactionLogger construction); headless detection: no Wayland, RDP misclassified, SSH+X misclassified; ProviderCache: empty values uncacheable-on-disk but memory-ok, aged disk entries immediately expired (disk re-hit per read), update_async TOCTOU, blocking sync I/O in async; registry reverse-index never updated + dead serialization; MODEL_PRICE_* invalid env silently ignored (vs sibling validators that warn); mask_credential stats the FILESYSTEM per call; no cross-process usage.json coordination; duplicate imports across 5 usage files; TrackingEngine.acquire dead; acquire/release lock mismatch (correctness rests on 1s polls); ReauthCoordinator.get_status exposes full credential paths; session tracker does synchronous full-history hashing under ONE global RLock (D17 serialization); ambiguous percent-cooldown encoding; custom-cap env keys can't express dots; get_retry_after parses bare year from dates.

---

## R10 — TESTS & DOCS META-AUDIT

### HIGH
- [H] The suite INSTITUTIONALIZES known bugs as contract: Responses `[DONE]` pinned in 6 assertions incl. failure paths labeled "protocol-valid" (test_responses_streaming.py:253/325/352/396/674/887 + test_w5:230-236 + the manual-test-guide:46 instructs the user to VERIFY the bug at the merge gate); store-failure-on-successful-stream RAISE pinned with pytest.raises in two tests (test_responses_streaming.py:571-610 — exactly the R7-H defect locked in); Anthropic double-subtract pinned with cache-small unrealistic numbers (test_usage_accounting.py:149-163 — real warm-cache shape `input 127 + cache_read 2727` yields 0 under the pinned formula); `minimal` effort pinned (test_w4:130-141); `transport=="sse"` pinned inside the WebSocket formatter (test_stream_transport.py:22); drifted `keep` vocabulary pinned (test_w13:191-241); old D13 bare-name rule pinned (test_w_prof:130-139).
- [H] Fabricated-fixture family (the recurring failure mode): message_delta usage with input tokens (real wire: input on message_start — masks the R5/R6 replace-merge bug; test_w5:403/489 vs matrix's REAL shape at :41 — two fixtures encode CONTRADICTORY wire contracts); index-less hand-built stream events for the three-block test (test_w4:880-902 — the R6-B trigger is exactly the missing indexes); non-existent `message.delta` dotted type (test_w4:885 — live parsers emit `message_delta`); fictional `costDetails` gemini field (zero src hits; the "usage_and_cost_parity" test never asserts cost — native `: cost` drop invisible); bare `[DONE]` fed to gemini streams (real gemini never sends it); `thinkingLevel` lowercase pinned; env example key `endpoint_paths` vs profile `endpoint_path` pinned per surface.
- [H] Executor-level EMBEDDINGS tests: ZERO (the R2 BLOCKER is structurally invisible — no test touches aembedding/build_embedding/operation dispatch).
- [H] Thinking-handler/transform tests pass while native integration is dead (unit-green, integration-dead — no test asserts the native path invokes transforms).
- [H] Global registry pollution across test files (register_*(replace=True) with no teardown — probe: adapters/protocols leak across files; subset-assertions mask ghosts).
- [H] config-reference documents THREE+ nonexistent knobs (REQUEST_LOGGING_ENABLED — real switch is a CLI flag; ADVISORY_MODEL_PRICING — real is MODEL_PRICE_*; FAIR_CYCLE_EXHAUSTION_THRESHOLD — real is EXHAUSTION_COOLDOWN_THRESHOLD; ROTATOR_LIBRARY_CONFIG vs LLM_PROXY_CONFIG_FILE; SKIP_OAUTH_INITIALIZATION vs SKIP_OAUTH_INIT_CHECK) + wrong defaults (GLOBAL_TIMEOUT 600 vs 30; FAIR_CYCLE_DURATION 86400 vs 604800 in THREE docs; MODEL_INFO_REFRESH_INTERVAL 3600 vs 21600; RESPONSES_STORE_FAILED false vs true) + conceptual errors (HOST/PORT as env though argparse-only; "startup-loaded only no hot reload" vs per-request re-parse reality; L3 semantics; stream tunables pointed at the wrong module; capture-set too vague to operate).
- [H] The untracked-33 problem sharpened: untracked test_error_handler.py covers only synthetic shapes and misses every G1 defect (quota-at-400, N1 shadowing, no-envelope 429, substring false positives) — false confidence in the exact subsystem being reworked; tracked-only suite = 1174.
- [H] manual-test-guide (the R11 instrument) bakes in: the [DONE] bug, a body key described as a header (x-proxy-estimate), stale test count (1321 vs 1333), wrong WS path.

### MEDIUM (selected)
- [M] No multi-chunk n>1 stream test anywhere (three known sibling bugs unpinned); no client-wire usage-shape assertions for non-chat clients (figures only); capture-set test self-referential (hand-written alias strings, one classify_error call — classifier drift won't fail it); zero pins for the four R3-HIGH routing bugs (@digest, @native-in-model, profile-blind promotion, pre-flight abort); .env.example accuracy test checks presence never absence (X-Proxy-Output-Protocol paragraph survives); x-proxy-conversion body-key tests will churn with no logs-only successor; registry-level can_inherit green while the engine-level same-model pool is dead (test_w13:78-86 asserts the precondition, never the behavior — probe: engine cross-provider = cache_miss); `insert+auto` declarative path untested (engine test bypasses with when_missing_only=False).
- [M] Three-way contradiction on custom caps (doc says clamp; config rejects >100%; tests pin no-clamp); divergent stale second-opinion draft inside the authoritative dir (untracked, differs from the _superseded copy — read by agents); grep-the-source tests (test_executor_session_forwarding asserts source strings, not behavior); tautological assertion (`assert "hello world" in ... or "hello" in output`).
- [M] README endpoint table missing 10+ live routes (responses family, gemini ingresses, quota-stats, model-info); DOCUMENTATION.md fair-cycle default wrong too.

### LOW / NIT
Untracked refactor tests build bare AsyncClients (future network risk); duplicate test names across files; stale test name (gemini stream route exists now); FAIR_CYCLE_QUOTA_THRESHOLD/RESET_COOLDOWN_THRESHOLD + OVERRIDE_TEMPERATURE_ZERO absent from all docs; heavy private reach-ins (~35 ._sessions etc.); reverse-order rerun GREEN (no cross-file order dependence — verified); conftest minimal; zero skips/xfails; tracked-only suite green at 1174; key-policy subprocess harness careful; L1-retention/redaction/store policies solid.


- R1: D1 at shell (every route passes own input_protocol, zero output-protocol refs); anthropic/gemini error vocabularies correct incl. gemini gRPC-status shapes; auth dependency ordering correct in all three launch modes; responses error envelopes spec-shaped; key-policy core loopback/prompt/block logic sound; startup display masking fixed-shape.
- R2: dispatch order (custom-first → native → litellm) + fail-closed explicit modes on both paths; raw fast-path gate conditions + traced canonical-rebuild overlays; accounting singularity non-stream; streaming fallback pre-output gating + heartbeat passthrough + visible-output lock; W13 merge precedence + weakening guard; trace redaction incl. api_key paths; include_usage explicit opt-out honored.
- R3: identity normalization CLEAN for well-formed inputs (bare provider reaches every sink; profile is provenance-only); grammar colon-in-model survival for ollama tags + openrouter :free on both parsers; resolve_profile core matrix (explicit-wins/default-match/unique-match/errors); policy hard-stop non-overridable + alias-laundering guard; per-target clone non-mutating at top level; field-cache identity provider+model.
- R4: registry discovery/aliases/instance-cache core correct (atomicity aside); neutral model W2 completeness incl. per-candidate stop; stop-reason tables for all documented OpenAI/Anthropic/Responses values; reasoning effort/budget round-trips for the engaged cases; tool-choice named/required/auto/none across four protocols; structured output chat/responses/gemini; validation gate same-protocol early-return + pairing checks; warning dedup plumbing; opaque-state gating (preserve_source AND provider-identity); ollama lossless same-protocol round-trip.
- R5: W7 staging both directions traced+tested; raw fast-path deepcopy discipline + gate semantics + overlay taxonomy; D11 _none-bucket keys + strict-session continuation + classifier fail-closed; engine mode round-trips + per-tool-call correlation; weakening guard DOES check compat/transform identity; both stores' TTL/LRU/atomicity; SSE decoder standard shapes; HTTP error-envelope preservation (RESOURCE_EXHAUSTED→quota_exceeded + quotaResetDelay retry parsing); adapter chain isolation/order/traces.
- R6: wait-loop gates live and correct (TTFB/stall/heartbeat cadence/disconnect-cancel, exception-safe task teardown); completion gate correct for anchors; terminal error frames per family via canonical formatter (mid-stream lifecycle reuse); gemini same-protocol multi-candidate per-index finishes; anthropic block open/close discipline + signature gating; client-wire usage merge field-wise correct; ChatWire hold-back/finish precedence; provider cost precedence incl. `: cost`/`event: cost`/scalar on the chat-wire path; visible-output lock + heartbeat-transparent rotation; no forced chat intermediate; formatting once at tail.
- R7: capability-token system solid (hash-only, constant-time, fail-closed identical 404s, no oracle; token regex covers the whole scope vocabulary); hybrid continuation core works (empty lineage → provider passthrough, non-empty → replay + pointer strip + disable flag honored); WS safety chain (aclose nesting, per-turn scope re-derivation, lifecycle rejects, spec-shaped miss frames, lifetime limit); store hygiene (deepcopy both ways, corrupt-JSON rows are misses except the expires_at hole, legacy migration); lifecycle validation + lineage cycle guard; non-stream create redaction + stored-request whitelist.
- R8: all 10 chat transport bases + endpoints + auth schemes verified correct against official docs + litellm reference (openai/openrouter/groq/mistral/cohere-compat/chutes/nanogpt/firmware/nvidia_nim/gemini incl. alt=sse + countTokens); quota-tracker endpoints correct (nano-gpt epoch-ms conversion, chutes, firmware); discovery mechanics (_-prefix skip, nvidia remap, transport-key rejection for code providers, JSON protocol validation, snapshot freeze); hook dispatch via signature inspection correct; resolve_profile matrix correct; registry key mapping consistent for 9/10 (nvidia the exception); DeepSeek custom-path stream accumulator correct.
- R9: usage recording exactly-once through one sink under one lock; window math correct for rolling/daily/weekly (monthly off-by-one noted); cooldown engine key coverage + backoff/preserve semantics; sequential stickiness symmetric; config validation strict where it exists (secrets/api_base/templates); resilient_io/zstd happy paths; session persistence load guards (schema/expiry/caps/namespace); transaction never-fail writes + retention + correlation; error taxonomy helpers (retry-after extraction incl. Google shapes); registry hashing stable.
- R10: full suite green forward AND reverse order (no order dependence); conftest minimal/global-state-free; zero skips/xfails; tracked-only 1174 green (untracked purely additive); key-policy subprocess harness careful; W3 byte-fidelity + W12 retention/redaction + W13 precedence pins genuine; profile grammar colon handling well-pinned; native include_usage + gemini-no-chat-options good narrow pins; env-example test pins the 10 defaults it covers correctly.

---

## R-CHAT — CHAT COMPLETIONS depth round (flash + seek; light PENDING — rate-limited)

### HIGH
- [H] Foreign annotation shapes leak onto the chat wire: `_format_openai_annotations` raw-replays ANY truthy `raw["type"]` with no shape guard — anthropic `char_location`/`web_search_result_location` and responses' FLAT `url_citation` replay verbatim; official chat schema is strictly nested `{"type":"url_citation","url_citation":{...}}` (openai_chat.py:1382-1385; both response and request-history directions; probe-verified).
- [H] Streaming audio output DESTROYED for chat's own native shape: `delta.audio` parses into an audio block but `_openai_delta` (streaming.py:856-888) emits ZERO frames for audio — stream/non-stream asymmetry (non-stream synthesizes message-level audio); official per-chunk `delta.audio {id,data,transcript}` is the documented audio-chat pattern.
- [H] Streamed tool-call arguments ALWAYS erased from session anchors: `_collect_anchors` reads `call.function.arguments` — canonical ToolCall has NO `.function` → arguments forced `""` → `tool_event` anchors hash differently from the next request's real args → agentic session continuity silently degrades for ALL streamed tool calls (stream_ops.py:719-722; non-stream path records correctly).
- [H] Gemini→Chat streamed tool calls all carry `index:0` (index-less ToolCalls get the per-event enumerate fallback with no cross-event bookkeeping) — two calls in separate frames merge into ONE corrupted call under official SDK accumulators (streaming.py:870-888; distinct from the known wrong-but-distinct index bug — this one is a constant collision).

### MEDIUM
- [M] Cross-protocol custom tool calls streamed as a malformed hybrid: `"type":"custom_tool_call"` (Responses-only spelling) inside the function envelope — non-stream maps correctly to `{"type":"custom","custom":{...}}`; stream path bypasses `_format_tool_call` (streaming.py:870-888).
- [M] Choice-level `logprobs` leaks onto the MESSAGE object on rebuild (parse stashes into `message.extra`, `_format_message` merges extra → `logprobs` on the message where the official schema has none; openai_chat.py:249-252/:712).
- [M] Explicit provider `finish_reason:"length"` overridden to `"tool_calls"` after any tool call (`_with_final_reason` contradicts its own docstring "final frame's own reason wins" — truncated tool-call JSON presented as cleanly complete; stream_ops.py:297-299 + same override log-side in the aggregator).
- [M] Client-explicit `logprobs:false` silently flipped to `true` when `top_logprobs` present (pairing satisfied by overriding the explicit value, disclosed as "synthesized" only; openai_chat.py:1234-1243).

### LOW / NIT
- [L] Stream usage frame omits `cost_details` (non-stream emits it — disclosure parity); `role` emitted for only the first choice (global `role_emitted` — official emits per choice); file_id-only image renders `{"url":"<file_id>"}` (not a URL/data-URI); tool messages carry non-spec `name` (official param is content/role/tool_call_id); `chatcmpl_<hex>` id underscore vs official `chatcmpl-` hyphen; same-protocol stream chunks lose provider identity (provider id parsed into `extra` but never read; `system_fingerprint`/`service_tier` not lifted; id/created/model re-minted — the concrete evidence behind the ledgered always-re-serialize note); legacy `function_call` streaming re-spelled as `tool_calls` even same-protocol (non-stream replays verbatim); `modalities` bare-string iterated char-by-char ("audio" → ['a','u','d','i','o']); contentless husk events (empty deltas/keepalives/malformed) open foreign stream lifecycles (anthropic message_start with zeroed usage on nothing); streamed delta-level annotations are dead data (parsed into extra, no formatter ever re-emits — stream citations vanish even chat→chat); aggregator log-shape: forced finish override + custom chunks hard-labeled "function" + choices[0]-only; `stream:"false"` string parses truthy; `usage` key omitted entirely when unknown (official completion objects always carry usage).
- Verified-solid additions: per-choice canonical events incl. per-choice finish/logprobs; legacy function_call/function-role NON-STREAM handling complete (verbatim replay + cross-protocol tool_use translation); usage detail buckets both directions + stream total backfill; content-null discipline; reasoning_content direction matrix; audio-format label map; same-protocol non-stream rebuild preserves id/created/model/fingerprint/tier + extensions.

---

## R-ANTHROPIC — MESSAGES depth round (flash + seek; light PENDING — rate-limited)

### HIGH
- [H] Same-protocol streams DESTROY non-whitelisted blocks and fabricate empty text blocks: official `mcp_tool_use`/`mcp_tool_result`/`container_upload` blocks → re-emitted as `content_block_start {"type":"text","text":""}` + stop — tool id/name/input and result content VANISH (whitelist contains a FICTIONAL `mcp_toolset_tool_result` that can never match real wire; stream formatter has no raw-replay branch though non-stream has one; streaming.py:303-405 + anthropic_messages.py:657-668).
- [H] Stream path emits `redacted_thinking`/encrypted reasoning to FOREIGN providers UNGATED (`_anthropic_block_start` redacted branch has no `may_emit_opaque_provider_state` check anywhere in `_format_anthropic` — Responses encrypted_content laundered into anthropic-typed redacted_thinking; guaranteed signature 400 + cross-provider state leak; non-stream gates correctly — stream/non-stream asymmetry; streaming.py:946-950).
- [H] `server_tool_use` input deltas fabricate a bogus client-callable `tool_use` block: open-block kind association lost + builtin blocks never registered in open_blocks → `input_json_delta` reopens a new empty-named tool_use (litellm fixed this exact ecosystem bug in PR #17798; same-protocol corruption; to chat it also emits name:null tool_calls; anthropic_messages.py:998 + streaming.py:335-363).

### MEDIUM
- [M] `redacted_thinking` dropped from same-protocol CLIENT responses on the canonical-rebuild path (redacted branch ignores `client_direction=True` — the cache-and-replay scenario it exists for loses the block; anthropic_messages.py:701-713 vs :714-730 which handles it correctly).
- [M] `tool_choice:{"type":"none"}` treated as ILLEGAL + tools omitted (official supports it since Feb 2025; the "Anthropic cannot accept" comment is false; omitting tools changes token accounting; anthropic_messages.py:834-880 + canonical.py:965-969).
- [M] `document` blocks with `source.type:"content"` malformed on rebuild (media parser has no content branch → injected `data:""` key that the official ContentBlockSource doesn't define; content array LOST cross-protocol; anthropic_messages.py:1192-1277).
- [M] `count_tokens` 400s on server/hosted tools AND PDF/document inputs (chat projection rejects server tool types at validation; litellm token_counter rejects file items — official counts tools/images/documents; client/anthropic.py:141-156 + main.py:1144-1148; both auditors independently).
- [M] Foreign citation shapes leak BOTH directions (anthropic `web_search_result_location` replayed verbatim onto chat/responses wires; responses `url_citation` streamed into anthropic `citations_delta` — the non-stream mapper `_format_anthropic_citation` exists; stream + foreign-target paths bypass it; chat/responses raw-replay lacks the target-protocol guard).
- [M] Tool-definition extension fields vanish silently cross-protocol (`strict`/`input_examples`/`defer_loading`/`allowed_callers` — only cache_control disclosed; Responses misses `strict`, a missed level-1 mapping since chat maps it).
- [M] Terminal `message_delta` discards officially documented usage members (input + both cache fields dropped by the `output_only` projection — real observed data discarded; official streaming examples carry all members; streaming.py:1220-1236).

### LOW / NIT
- [L] Stream citations branch replays foreign raw without a preserve-source gate (latent until a foreign source grows citations); response `container` object dropped on streams (code-execution clients lose identity/expiry); same-protocol `message_start.id` synthesized, never the upstream id (facade's own fidelity contract violated on streams; three id disciplines for one surface); chat `parallel_tool_calls:false` not mapped to `disable_parallel_tool_use` (reverse works — asymmetric); `stop_reason` rebuild loses `model_context_window_exceeded` (→ max_tokens) and `compaction` (→ end_turn + warning) — native value sits in metadata, never restored; non-stream response omits the REQUIRED `usage` object when provider reports none (SDK model_validate fails; stream already emits zeros); fabricated redacted-thinking fixture (`signature` key — real wire uses `data`; pins a non-existent shape).
- Verified-solid additions: thinking stream lifecycle spec-correct (delta/signature flush ordering, display:"omitted", redacted one-shot); complex same-protocol round-trip byte-faithful incl. cache_control/ttl/containers/mcp_servers/output_config; stop vocabulary all 7 official values end-to-end; usage cache epochs fold/unfold; thinking parameter surface vs current effort/thinking guides; error envelope vocabulary official (minus the known proxy_timeout gap); auth dual-header correct.

### LIGHT-ROUND ADDITIONS (R-CHAT light, beyond flash/seek above)
- [H] Non-stream `choices: []` emits an EMPTY SUCCESS (format_response has no empty-choices gate; validation only gates stop==error — official requires ≥1 choice; openai_chat.py:344-372).
- [H] Chat `file` parts (file_id-only AND file_data) → Anthropic build `messages: []` SILENTLY (file falls through the content formatter with no append and no warning; validation passes on file_id — illegal empty conversation guaranteed 400; anthropic_messages.py:744-748 — gemini at least warns media_dropped).
- [H] `_aggregate_chat_chunks` CONCATENATES n>1 candidates (content "A"+"B" → "AB" single choice — D9's forbidden concatenation, log/reconstruction side; route_helpers.py:124-135).
- [M] `file` part drops `prompt_cache_breakpoint` SAME-PROTOCOL (text/image branches preserve via extra; file branch rebuilds bare — D4 violation); `prompt_cache_breakpoint` dropped cross-protocol ALL kinds with zero disclosure (validation discloses cache_control only).
- [M] Legacy `functions`/`function_call` never canonicalized (stay in extra; cross-protocol loses tools ENTIRELY — anthropic gets tools=None; deprecated-but-live SDK surface).
- [M] Custom-tool `format:{grammar}` lost chat→responses undisclosed (definition-side twin of the allowed_names bug).
- [M] `input_audio format:"wav"` label → gemini `mimeType:"wav"` (response-audio normalizes labels to MIME at :590; the input path doesn't — official gemini mimeType is a full MIME).
- [M] Moderation-only stream chunks swallowed (documented member; formats to zero frames same-protocol).
- [M] Responses→Chat streamed tools ALSO collide at index:0 (`event.output_index` never consulted — same root as the gemini collision, second source).
- [M] ChatWire single-slot hold-back loses per-choice finishes for n>1 (held reason overwritten per chunk; per-choice reasons must survive).
- [M] Route treats `stream:"false"` (string) as streaming — truthiness at main.py:675 (non-stream request SERVED AS SSE; independent from the parse-side instance).
- [M] Missing/null non-stream finish_reason defaults to `stop` silently (both-None branch skips the warning).
- [M] Aggregator drops `system_fingerprint`/`service_tier`/`moderation`/`logprobs` from assembled responses; file_data-without-mimeType → gemini builds `contents: []` (guaranteed upstream 400 instead of proxy-side reject).
- [L] `obfuscation` delta dropped both directions (parsed into extra, never re-emitted — security-mitigation field stripped same-protocol); legacy `finish_reason:"function_call"` silently → `tool_calls` same-protocol; synthesized audio fabricates `transcript:""`; n>1 same-protocol streams split one chunk into N frames (framing change); cross-protocol choice logprobs replayed without preserve_source gate; `url_citation` missing `url` emitted malformed (None-keys dropped, required fields absent).

### LIGHT-ROUND ADDITIONS (R-ANTHROPIC light, beyond flash/seek above)
- [H] `model_context_window_exceeded` collapsed to `max_tokens` in the CANONICAL TABLE itself (canonical.py:48 — distinct handler semantics: cap-raise vs input-reduce; same-protocol round-trip destroys it; the metadata-restore item is the sibling).
- [H] Effort `xhigh`/`max` (valid Anthropic levels) DOWNGRADED to `high` with double warnings; `minimal` (invalid) passes through verbatim — the INVERSE of the known minimal bug (anthropic_messages missing from `_EFFORT_NATIVE_PROTOCOLS`; official vocab is low/medium/high/xhigh/max).
- [H] Mid-conversation `role: system` messages (2026 API) dropped on rebuild incl. per-message `output_config.effort` (message-level output_config not modeled anywhere; empty-content early-continue discards it; false `instructions_merged` warning describes a merge that didn't happen).
- [M] `compaction` stop_reason: stream terminal emits ILLEGAL `stop_reason: null` (indistinguishable from no-reason-observed) + approximation warning names 'unknown' not the native value.
- [M] Streaming refusal terminal DROPS `stop_details` (non-stream preserves it via extra; official delivers stop_details on the terminal message_delta).
- [M] count_tokens: estimates where upstream 400s (server tools → ProtocolError ESCAPES the route catch as 500 api_error, not protocol 400; url-source images succeed where upstream rejects) + thinking-strip semantics INVERTED for current flagships (4.6+ bills prior-turn thinking as input — unconditional strip assumption stale).
- [M] Usage members `inference_geo`/`speed`/`iterations` + full `output_tokens_details` dropped in extract+format (official Usage carries them; same-protocol clients lose residency/speed/iteration observability).
- [M] `content_block_location`/`search_result_location` citation positions lost in canonical form (start/end/document_index all None; only the raw passthrough keeps them — cross-protocol mappers see nothing).
- [L-M] `tool_use.caller`/`toolset_name` dropped cross-protocol undisclosed (security-relevant: determines who may execute the call).
- [L] Stream delta block types inconsistent (`thinking_delta`/`signature_delta` vs non-stream `reasoning` — one logical block, three canonical spellings); `529/overloaded_error` and `billing_error` misclassified (529 → generic server_error instead of retryable overloaded; billing → invalid_request); provider `request_id` never emitted in error payloads; `metadata.user_id` forwarded into chat payloads undisclosed + unvalidated.

---

---

## R-GEMINI — GEMINI v1beta depth round (light + flash + seek — COMPLETE)

### HIGH
- [H] Streaming `partialArgs`/`willContinue` function-call args DESTROYED (adapter reads only `args`; probe: two partialArgs frames → args fully lost + call DUPLICATED as two empty calls; non-stream partialArgs-only → ValueError 500) — official Vertex streaming-FnC contract unhandled (streaming.py:532-677, gemini.py:577-594/:826).
- [H] `functionCall.willContinue` + `functionResponse` `willContinue`/`scheduling`/`parts` dropped even SAME-PROTOCOL (2026 members; rebuild replaces the raw inner objects) — `parts` known, the other two new (gemini.py:819-862).
- [H] countTokens nested `generateContentRequest` form broken BOTH directions: build emits BOTH contents AND the envelope (official: mutually exclusive, contents ignored when envelope set; envelope itself lacks contents/model — server 400s per googleapis/python-genai#432); ingress can't PARSE the SDK's nested form (zero messages → silent near-zero 200 or 400) (gemini.py:213-223, client/gemini.py:60-95).
- [H] Raw fast path LEAKS ILLEGAL `model` key into gemini upstream body (rebuild pops model/stream as unknown-field 400s; raw path replays + overlay rewrites but never removes; the W3 residual claiming "canonical builder emits it too" is now stale) (native_provider/executor.py:86-91 vs gemini.py:149-152).
- [H] `computerUse`/`mcpServers` (+`enterpriseWebSearch`/`exaAiSearch`/`parallelAiSearch`/snake `google_maps`) unmodeled: parsed to fabricated "gemini_tool" name, hard-REJECTED cross-protocol though native Gemini tools; Responses computer_use_preview/mcp can never round-trip (gemini.py:691-710, validation.py:250-283).
- [H] Stream vs non-stream thoughtSignature gating DISAGREE (stream gates on protocol-equality + latched source_protocol; non-stream on provider-identity): cross-provider same-protocol streams replay signatures while non-stream suppresses; mixed-source streams gate differently per part kind (streaming.py:198-201 vs canonical.py:181-202) — D8 is provider-identity, not protocol-equality.
- [H] `skip_thought_signature_validator` sentinel NEVER emitted on live paths (grep: only _retired) — every cross-protocol agentic conversation → Gemini 3 first tool turn = deterministic 400 (foreign-source client-history variant; extends G3's post-strip scope; official docs sanction the dummy signature for exactly this) (gemini.py:819-837 + streaming.py:657-677).
- [H] Gemini-source structured output ships UPPERCASE OpenAPI schema types + FABRICATED `strict:true` to chat/responses (schema mis-spelled AND illegal under the strict mode it just acquired — guaranteed 400; litellm keeps separate builders for exactly this) (canonical.py:1044-1125).
- [H] Same-protocol streams DROP response-level metadata (`responseId`/`promptFeedback`/`modelStatus` — a BLOCKED prompt's blockReason vanishes) + part-level (`videoMetadata`/`partMetadata`/`mediaResolution`/`audioTranscription`) + future part types silently (non-stream replays verbatim) (streaming.py:512-713).
- [H] Gemini→chat streamed tool calls emit `finish_reason:"stop"` not `"tool_calls"` (promotion applied to UnifiedResponse only, never per-event; non-stream correct) (gemini.py:289-291 vs :428-449).

### MEDIUM
- [M] `displayName` dropped on inlineData/fileData even same-protocol (breaks multimodal `$ref` round-trips; litellm types also lack it but official has it).
- [M] Usage detail arrays (`promptTokensDetails`/`cacheTokensDetails`/`candidatesTokensDetails`/`toolUsePromptTokensDetails`) + `trafficType`/`serviceTier` dropped same-protocol rebuild (non-stream) AND stream (stream ALSO drops `toolUsePromptTokenCount` — non-stream keeps; three auditors, all paths).
- [M] Per-candidate extras (safetyRatings/citationMetadata/avgLogprobs/logprobsResult/finishMessage/tokenCount) dropped gemini→foreign NON-STREAM undisclosed (stream drops known; non-stream path new).
- [M] Chat→gemini annotations always dropped though `citationMetadata`/`groundingMetadata` homes EXIST (reverse direction is mapped — asymmetric, not unrepresentable).
- [M] `VALIDATED` tool-mode allowlist lost at parse (`allowedFunctionNames` ignored — foreign targets get unconstrained auto; same-protocol masked by verbatim replay).
- [M] Hosted-envelope params flattened to `{}` foreign→gemini (googleSearch type/timeRangeFilter, fileSearch stores lost silently).
- [M] Logprobs mapping missing BOTH directions (exact mapping exists: logprobs↔responseLogprobs, top_logprobs↔logprobs; disclosed-drop doesn't substitute).
- [M] `extract_usage` misses `responseTokenCount` alias; fallback total omits the toolUsePrompt bucket.
- [M] `VIDEO` response modality hard-rejected though supported (litellm enum: TEXT|IMAGE|AUDIO|VIDEO).
- [M] `candidateCount>1` passes to `streamGenerateContent` unchecked (official: multi-candidate not supported on stream).
- [M] Retry timing lost: google.rpc `details[]` (RetryInfo/QuotaFailure/ErrorInfo) DROPPED by the shell renderer (envelope rewritten from error_type; original on `.response` never consulted) + `retry in` phrasing unparsed by the duration parser.
- [M] Thinking-off emits the known-invalid `thinkingBudget:0` shape on Gemini 3 (cannot disable on 3.1 Pro — model-aware handling needed, not a "verify model" warning).
- [M] `fileData` emitted WITHOUT required `mimeType` (upstream 400, zero warning — the inlineData path warns; asymmetric) (gemini.py:1223-1259).
- [M] `text/x.enum` + responseSchema folded into json_schema (enum mode LOST; OpenAI object-root rule → deterministic 400; the code comment claims this never happens).
- [M] Media part `thoughtSignature` BYPASSES the opaque-state gate (third bypass site: text + functionResponse known, media new; cross-provider leak probe-confirmed).
- [M] `toolConfig` siblings (`streamFunctionCallArguments`/`includeServerSideToolInvocations`) dropped cross-protocol with zero disclosure.
- [M] `urlContextMetadata` never lifted anywhere — url-only candidate becomes an EMPTY SUCCESS with zero disclosure cross-protocol (grounding's twin IS implemented).
- [M] No gemini-native model discovery ingress (`GET /v1beta/models[/{id}]`) — google-genai/Gemini-CLI clients 404 on `.models.list()`, the first thing they do.
- [M] False "no safe mapping" warning on legal same-protocol `responseMimeType: text/x.enum` (retain-warning fires though the value IS replayed via extensions); same false-positive class for `responseFormat` (mapped set omits it).
- [M] Empty candidate list yields an empty 200 success (gemini twin of the chat empty-choices gate).

### LOW / NIT
- [L] Part-level extras silent cross-protocol (partMetadata/mediaResolution/videoMetadata on text+media parts); dead `generationConfig` drop loop (unreachable + false "RECORDED — never silent" comment); misleading `xhigh` warning text ("budget table" for a level mapping); `labels` replayed to the Gemini API where the SDK rejects them (strip-with-warning for AI-Studio host); `responseFormat` array form (js-genai) unparsed; countTokens local estimate is text-only for media; `responseModalities` case-mutated same-protocol (["text"]→["TEXT"]); synthesized candidates always `"index":0`; usage-husk frames fabricate an empty candidate.
- Audit-brief items RESOLVED: `safetyConfig`/`contextCompression` are not generateContent fields (Live API only — N/A); `thinkingLevel` LOWERCASE is the reference spelling (litellm types + REST examples — clears the R4 needs-live-verification flag).
- Verified-solid additions: toolCall/toolResponse generic parts byte-faithful both paths; candidate metadata same-protocol non-stream via extra; unknown generationConfig keys disclosed cross-protocol (14-warning probe); tool-choice vocabulary incl. VALIDATED round-trip (minus M6); thinkingConfig 0/-1/levels/includeThoughts parse+rebuild; grounding→chat/anthropic citations mapped; promptFeedback.blockReason non-stream correct; per-candidate stream finishes + usage-on-first (D9); chat→gemini tool pairing (no fabricated ids); google.rpc envelope shape correct at the canonical formatter; ingress auth all three carriers; countTokens response round-trip.

---

## SWEEP STATUS — COMPLETE (2026-09-11)
- ALL 13 parts done: R1-R10 + R-CHAT + R-ANTHROPIC + R-GEMINI (each a full trio: light + flash + seek).
- Next: fold into fix-pass-plan.md (Round 2 integration section), then stop.
